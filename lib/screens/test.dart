import 'package:flutter/material.dart';

class TestInitailiza extends StatelessWidget {
  const TestInitailiza({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [],
      ),
    );
  }
}
